"""InfernoTactics domain and application packages.

The legacy ``env``, ``models``, and ``train`` packages remain importable while
the project is migrated behind explicit domain interfaces.
"""

__version__ = "0.1.0"
